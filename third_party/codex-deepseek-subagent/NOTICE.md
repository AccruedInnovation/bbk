# Third-party notice

BBK includes an unmodified, revision-pinned copy of the upstream
`Utopia-V/codex-deepseek-subagent` source for provenance and review. The
upstream project is MIT licensed; its complete license text is retained in
[`LICENSE`](LICENSE). No upstream installer, hook, agent, or skill is
activated by this registry-only contribution.

The pinned files are identified by their upstream and local SHA-256 digests in
[`UPSTREAM-FILE-MAP.csv`](UPSTREAM-FILE-MAP.csv). That map records the one
explicit transformation and its operation count; the complete transformation
record is in [`UPSTREAM.json`](UPSTREAM.json). BBK-owned registry and schema
files are outside that pinned source set.
