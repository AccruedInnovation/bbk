{
  "schema_version": "bbk.prompt-module.v1",
  "id": "bbk-prompt-durable-handoff",
  "title": "Structured return and durable exact handoff",
  "description": "Prefer structured role returns and use one verified durable carrier only when exact state materially requires it.",
  "clauses": [
    {
      "id": "HANDOFF.STRUCTURED_RETURN_FIRST",
      "text": "Use the structured role result when the channel carries it losslessly; do not package every return."
    },
    {
      "id": "HANDOFF.MATERIAL_PACKAGE",
      "text": "Create `bbk.handoff.v2` only for large/truncation-sensitive output, binary content, cross-process/session/host transfer or durable recovery, schema/external-interface need, or exact artifact/evidence closure unsafe in the role result."
    },
    {
      "id": "HANDOFF.BIND",
      "text": "For a material package, use the BBK package engine to bind safe project-relative paths, exact subject/revision, producer attempt, disposition, canonicalization, manifests, hashes, byte counts, and receipt. Do not rebuild generated identity fields with shell commands."
    },
    {
      "id": "HANDOFF.VERIFY_ONCE_REUSE",
      "text": "Producer seals and verifies once. Consumers validate the current verifier receipt and expected binding. Crossing role/process/session/orchestration boundaries alone does not trigger a rerun; rerun only after changed bytes/declared keys, missing/mismatched receipt, observed corruption, or an explicitly justified independent method."
    },
    {
      "id": "HANDOFF.SEPARATE_STATE",
      "text": "Keep physical-attempt disposition, semantic readiness, accountable acceptance, finding closure, completion, and release as separate fields and authorities."
    },
    {
      "id": "HANDOFF.HISTORY",
      "text": "Preserve partial, failed, blocked, cancelled, stale, superseded, and predecessor state. Never rewrite a published record to make a successor look originally successful."
    },
    {
      "id": "HANDOFF.CHANNEL_LIMIT",
      "text": "Use live messages for brief coordination and verified references. A lossless structured result needs no package; chat cannot replace a required exact carrier."
    }
  ]
}
