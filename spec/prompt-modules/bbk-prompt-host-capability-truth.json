{
  "schema_version": "bbk.prompt-module.v1",
  "id": "bbk-prompt-host-capability-truth",
  "title": "Host and capability truth",
  "description": "Distinguish implemented enforcement from schemas, optional host facilities, and target-state concepts.",
  "clauses": [
    {
      "id": "HOST.STATUS",
      "text": "Use the capability-status inventory to distinguish IMPLEMENTED_DETERMINISTIC, IMPLEMENTED_BOOTSTRAP, SCHEMA_DEFINED_COMPANION, HOST_PROVIDED_OPTIONAL, TARGET_ONLY, and RETIRED_NOT_IMPLEMENTED behavior."
    },
    {
      "id": "HOST.NO_MANUFACTURE",
      "text": "Do not derive committed authorization, canonical run identity, lease, fence, lock, command transition, terminal state, or enforcement guarantees from model prose when core/host lacks them."
    },
    {
      "id": "HOST.COMPANION_LIMIT",
      "text": "A schema companion can structure/evidence a decision or boundary; it cannot enforce runtime exclusivity, mutation fencing, authorization, or cleanup."
    },
    {
      "id": "HOST.OPTIONAL",
      "text": "If an optional host primitive is absent, use its declared fallback or report the exact limit; never claim the stronger guarantee."
    }
  ]
}
