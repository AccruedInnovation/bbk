Use `followup_task` on the same logical `bbk_worker` child from MH-CODEX-01. Do not spawn a replacement child.

The follow-up is within the same role, scope, authority, procedure selection, profile, harness, and mutation ownership. Append exactly `followup-reused` plus one LF to `input.txt`, run `python check.py --followup`, and return structured completion.

The unchanged compiled procedure set must be reused. Do not read or search the canonical procedure source and do not expose the compiled procedure as an external skill. Preserve the raw JSONL log and identify the follow-up reuse event.
